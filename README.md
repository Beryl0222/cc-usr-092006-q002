# 景区客流联动服务

服务面向大型景区值班室，区域占用量会受到闸机、停车、摆渡和图像计数等多种信号影响。消防容量是不可突破的安全边界，数据来源中断时也需要明确展示信息的新鲜程度。

`fixtures/zones.json` 记录区域及其已接入的计数来源，`src/domain.py` 提供基础读取逻辑。资料中的容量值用于本地开发示例，不代表任何真实景区。

基础检查可运行 `python -m unittest discover -s tests`。
